# 非敏感默认项：提交到 git，新 Agent 启动时由 scripts/cloud-sync-env.sh 写进工作区 .env。
# 同名 Secret 覆盖这里的值。密钥类变量只配 Cursor Environment Secrets，绝不写进本文件。
#
# 这里逐字对齐本机 .env 的非敏感部分（deepseek anthropic 兼容端点），
# 于是云端只剩 ANTHROPIC_API_KEY 一项需要人工填 Secret。
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
OPENAI_BASE_URL=https://api.deepseek.com

# 执行与识图同走 deepseek-flash（V4.1-Flash 自带 Vision），与本机 .env 逐字同档。
#
# 此前这里不钉 AGENT_MODEL，理由是「不该由环境配置替使用者决定模型档位」。理由仍成立，
# 前提没了：本机 .env 已经显式钉死 Flash。不钉的结果不是「两边同走代码默认」，而是
# 云端跑 claude-opus-4-8、本机跑 Flash——两个档，正是当初要避免的那种悄悄换模型
# （此前钉 deepseek-chat 实测同一句话只出 1 个输出 token，claude-opus-4-8 出 93）。
# 所以现在两边都显式钉同一个。
AGENT_MODEL=deepseek-flash

# 不钉 AGENT_VISION_MODEL：执行者自己会看图，不另引识图角色（与本机 .env 一致）。
# 旧钉的 deepseek-v4-flash 官方已把该名字对应的模型退役，请求由 V4.1-Flash 承接。
# AGENT_VISION_MODEL=
